const chapter5 = {
  id: 5,
  title: "Концепции ООП",
  subchapters: [
    {
      id: "5.1",
      title: "Что такое ООП и зачем оно нужно?",
      content: `  
ООП) — это подход к программированию, при котором программа строится из объектов, которые взаимодействуют друг с другом

Основные идеи ООП:
 • программа ближе к реальному миру 
 • код становится понятнее и удобнее для поддержки
 • большие проекты легче расширять

В реальной жизни мы думаем объектами: человек, машина, телефон
В ООП — так же

Перед тем как изучать ООП, важно понять, что это не единственный способ писать программы

Основные подходы:
 1. Процедурное программирование
Программа — это набор функций и команд, которые выполняются по порядку
 2. Объектно-ориентированное программирование (ООП)
Программа — это набор объектов, которые взаимодействуют между собой


**Пример процедурного подхода**
\`\`\`name = "Илья"
age = 15

def say_hello(name):
    print("Привет, меня зовут", name)

say_hello(name)
\`\`\`
       `
    },
    {
      id: "5.2",
      title: "Атрибуты и методы",
      content: `
Атрибуты — это переменные, которые хранятся внутри объекта и описывают его состояние. Они могут быть связаны с классом или его экземпляром. 

В Python существуют два основных типа атрибутов:
Атрибуты экземпляра — принадлежат конкретному объекту (экземпляру класса). Определяются внутри методов (обычно в __init__) и доступны через self. 
Например, self.name = name, self.age = age в классе Person.
Атрибуты класса — определены на уровне класса и разделяются между всеми его экземплярами. 
Определяются непосредственно внутри класса, но вне любых методов. Например, school = "Школа №1" в классе Student.
 
Особенности атрибутов:
Атрибуты класса общие для всех экземпляров, а атрибуты экземпляра уникальны для каждого объекта. 

Python хранит их в отдельных словарях: __dict__ класса и __dict__ экземпляра. 

Для организации атрибутов используются соглашения об именовании: name — публичный атрибут, доступный извне класса; _name — защищённый атрибут (соглашение, не механизм защиты); __name — приватный атрибут (подвергается изменению имени — name mangling). 

Методы — это функции, определённые внутри класса, которые описывают поведение объекта. Они определяют, какие действия может выполнять объект. 

Особенности методов:
Объявляются с помощью ключевого слова def.
Первым параметром всегда идёт self — ссылка на конкретный объект, для которого вызывается метод. 
Через self метод может обращаться к атрибутам и другим методам того же объекта.
В зависимости от назначения и способа работы методы в ООП делятся на несколько типов:
Методы экземпляра — самый распространённый тип. 
Получают в качестве первого аргумента ссылку на экземпляр класса (self) и могут обращаться и модифицировать атрибуты этого экземпляра.
Методы класса — работают с классом в целом, а не с конкретным экземпляром. 
Получают в качестве первого аргумента ссылку на класс (cls) и могут обращаться только к атрибутам класса, но не к атрибутам экземпляров. 
Определяются с помощью декоратора @classmethod.
Статические методы — не имеют доступа ни к атрибутам экземпляра, ни к атрибутам класса. Действуют как обычные функции, но логически связаны с классом. 
Определяются с помощью декоратора @staticmethod.
Специальные методы (магические методы) — методы с особыми именами, которые вызываются автоматически при определённых операциях (например, __init__, __str__, __repr__).


2.1 Атрибуты (данные внутри объекта)

///class Dog:
  def __init__(self, name, age):
    self.name = name
    self.age = age

dog1 = Dog("Барсик", 3)
print(dog1.name)
///

Объяснение:

__init__ вызывается автоматически при создании объекта.
self — это ссылка на конкретный объект (dog1).
self.name = name — сохраняем значение внутри объекта.
Когда мы пишем:

///dog1 = Dog("Барсик", 3)
///

Происходит:

Создаётся новый объект
Вызывается __init__
Внутри объекта появляются:
name = "Барсик"
age = 3
dog1.name — это доступ к данным внутри объекта.
 

2.2 Методы (поведение объекта)

///class Dog:
    def __init__(self, name):
        self.name = name

    def bark(self):
        print(f"{self.name} говорит: Гав!")

dog1 = Dog("Рекс")
dog1.bark()
///

Объяснение
bark — это функция, связанная с объектом.
self.name берёт имя именно этого объекта.
Когда вызывается:

///dog1.bark()
///

Поэтому self — это dog1.
 


      `
    },
    {
      id: "5.3",
      title: "Конструктор __init__ ",
      content: `


Конструктор в Python — это специальный метод __init__, который автоматически вызывается при создании нового экземпляра класса. 
Его основная задача — инициализировать объект, то есть установить начальные значения его атрибутов.

///class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

p = Person("Анна", 25)
///  
     
Объяснение:

Конструктор нужен, чтобы:
сразу задать начальные значения
гарантировать, что объект создан корректно
Без __init__ пришлось бы писать:

///p = Person()
p.name = "Анна"
p.age = 25
///

Это менее безопасно и удобно.


4. 
      `
    },
    {
      id: "5.4",
      title: "Инкапсуляция",
      content: `

///class BankAccount:
    def __init__(self, balance):
        self.__balance = balance

    def deposit(self, amount):
        self.__balance += amount

    def get_balance(self):
        return self.__balance

account = BankAccount(1000)
account.deposit(500)
print(account.get_balance())
///

Объяснение:

__balance — “приватное” поле (Python его переименует внутри).

Прямой доступ:

///account.__balance  # ошибка
///

Почему это важно:

нельзя случайно изменить баланс
все изменения проходят через методы

Например, можно добавить проверку:

///def deposit(self, amount):
    if amount > 0:
        self.__balance += amount
///

`      
    },
    {
      id: "5.5",
      title: "Наследование",
      content: `
    
Наследование в Python — это механизм объектно-ориентированного программирования (ООП), который позволяет создавать новый класс на основе уже существующего. 
Такой новый класс называется производным (дочерним), а исходный класс —  базовым (родительским). 
Дочерний класс автоматически наследует атрибуты и методы родительского класса, а также может добавлять собственные особенности или модифицировать унаследованное поведение.     
      
Зачем нужно наследование?

Повторное использование кода. Позволяет избежать дублирования кода, так как общий функционал может быть определён в базовом классе и унаследован потомками. 

Создание иерархий. Помогает строить сложные взаимосвязанные системы классов, отражающие естественные отношения между объектами. 

Расширяемость. Даёт возможность легко добавлять новую функциональность в дочерние классы. 

Полиморфизм. Позволяет использовать объекты разных классов единообразно.

///class Animal:
    def speak(self):
        print("Животное издаёт звук")

class Dog(Animal):
    def speak(self):
        print("Собака лает")

dog = Dog()
dog.speak()
///

Объяснение:(

Dog(Animal) — означает, что Dog наследует Animal
Все методы Animal доступны в Dog
Но здесь мы переопределили метод:
Python сначала ищет speak в Dog
если не находит — ищет в Animal
Это называется переопределение метода (override)

 
`
    },
    {
      id: "5.6",
      title: "Полиморфизм",
      content: `
Полиморфизм (от греч. «много форм») — это один из ключевых принципов объектно‑ориентированного программирования (ООП), 
который позволяет использовать один и тот же интерфейс для разных типов данных или объектов. 
Проще говоря, это способность объектов разных классов реагировать на одинаковые методы или операции по‑разному.

Полиморфизм делает код более гибким, читаемым и масштабируемым: 
можно писать функции и методы, которые работают с объектами разных классов, не заботясь об их конкретной реализации.


///class Cat:
    def speak(self):
        print("Мяу")

class Dog:
    def speak(self):
        print("Гав")

animals = [Cat(), Dog()]

for animal in animals:
    animal.speak()
///

Объяснение:

У разных классов есть метод с одинаковым именем
Python не проверяет тип — он просто вызывает метод

Это называется “утиная типизация”:

“Если объект ведёт себя как утка — значит, это утка”

Главное — не тип, а наличие метода.



`
    },
    {
      id: "5.7",
      title: "Абстракция",
      content: `

Абстракция — один из четырёх ключевых принципов объектно‑ориентированного программирования (наряду с инкапсуляцией, наследованием и полиморфизмом).

Суть абстракции — выделить главные, существенные характеристики объекта и скрыть второстепенные детали его реализации. Это позволяет:

упростить работу со сложными системами;

сосредоточиться на том, что делает объект, а не как;

создать чёткие интерфейсы взаимодействия между компонентами;

повысить гибкость и масштабируемость кода;

уменьшить дублирование кода за счёт общих шаблонов.

///from abc import ABC, abstractmethod

class Shape(ABC):
    @abstractmethod
    def area(self):
        pass

class Square(Shape):
    def __init__(self, side):
        self.side = side

    def area(self):
        return self.side ** 2

sq = Square(4)
print(sq.area())
///
Объяснение:

Shape — абстрактный класс (его нельзя создать напрямую)
@abstractmethod — метод, который ОБЯЗАН быть реализован
Если забыть реализовать:

///class BadShape(Shape):
    pass

BadShape()  # ошибка
///

      `
    },
    {
      id: "5.8",
      title: "Магические методы",
      content: `
Магические методы в Python (также известные как специальные методы или dunder-методы) — это методы, имена которых окружены двойными подчёркиваниями 
(например, __init__, __str__, __add__). Они автоматически вызываются интерпретатором при выполнении определённых операций или использовании встроенных функций.

///class Book:
    def __init__(self, title):
        self.title = title

    def __str__(self):
        return f"Книга: {self.title}"

book = Book("Python для начинающих")
print(book)
///
Объяснение:

__str__ определяет, как объект выводится


      `
    },
    {
      id: "5.9",
      title: "Композиция",
      content: `
Композиция в объектно-ориентированном программировании (ООП) на Python — это концепция, которая моделирует отношение «имеет» (has-a). 
Она позволяет создавать сложные объекты путём объединения объектов других классов. 
При композиции класс-композит (составной класс) содержит объекты других классов-компонентов.

Основные характеристики композиции:

Классы, содержащие объекты других классов, обычно называют составными (composite), 
а классы, используемые для создания более сложных типов, — компонентами (component). 

Составной класс не наследует интерфейс класса компонента, но может использовать его реализацию. 

Отношение композиции считается слабосвязанным. Это означает, что изменения в классе компонента редко влияют на составной класс,
а изменения в составном классе не влияют на класс компонента. 

При создании объекта класса-контейнера также создаются объекты включённых в него классов. 

Композиция позволяет повторно использовать код, добавляя объекты к другим объектам, 
а не наследуя интерфейс и реализацию других классов. 

///class Engine:
    def start(self):
        print("Двигатель запущен")

class Car:
    def __init__(self):
        self.engine = Engine()

    def start(self):
        self.engine.start()

car = Car()
car.start()
///

Объяснение:
Car содержит Engine
Это отношение “имеет”, а не “является”
Разница:
Наследование: “Собака — это животное”
Композиция: “Машина имеет двигатель”
Композиция гибче: можно заменить двигатель без изменения класса машины.
 
⸻
 
      `
    },
    {
      id: "5.10",
      title: "Практический пример (разбор)",
      content: `
///class Product:
    def __init__(self, name, price):
        self.name = name
        self.price = price

class Cart:
    def __init__(self):
        self.items = []

    def add_product(self, product):
        self.items.append(product)

    def total_price(self):
        return sum(item.price for item in self.items)

p1 = Product("Ноутбук", 1000)
p2 = Product("Мышка", 50)

cart = Cart()
cart.add_product(p1)
cart.add_product(p2)

print(cart.total_price())
///

Объяснение:
Product — модель товара
Cart — корзина
Внутри Cart:
items — список объектов Product
это пример композиции
делает:

берёт каждый товар
извлекает цену
суммирует

Почему это хорошо:

код легко расширить (скидки, налоги)
можно добавить новые типы товаров

Итог (смысл всех концепций)

ООП нужно не просто ради классов, а чтобы:

структурировать код
избегать дублирования
упрощать расширение программы
моделировать реальные системы
      `
    }
  ]
}



export default chapter5;